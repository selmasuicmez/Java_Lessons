package day26abstractioninterfacent;

public interface AC extends Engine {
	/*
	 An interface can be child of another interface but
	 when you create relationship between two interfaces 
	 do not use "implements" keyword use "extends" keyword.
	 */
	
	/*
	 Parent of an interface must be interface. Do not try to 
	 make a class parent of an interface.
	*/

}
